version: '3.8'

services:
  mission-control:
    build: .
    container_name: mission-control
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file: .env
    networks:
      - evolution-api-h4pg_default

networks:
  evolution-api-h4pg_default:
    external: true
